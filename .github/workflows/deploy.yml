name: FastAPI Docker CD

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Fix permissions before checkout
        # DB 파일 권한 때문에 checkout이 실패하는 것을 방지
        run: sudo chown -R yangarch:yangarch ${GITHUB_WORKSPACE} || true

      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          clean: false # 중요: assets.db 등 기존 파일을 지우지 않음

      - name: Check for configuration changes
        id: check_changes_step
        run: |
          # Docker 빌드가 필요한 파일 변경 감지
          if git diff --name-only HEAD^ HEAD | grep -E 'Dockerfile|docker-compose.yml|requirements.txt'; then
            echo "build_needed=true" >> $GITHUB_OUTPUT
          else
            echo "build_needed=false" >> $GITHUB_OUTPUT
          fi

      - name: Deploy with Docker Compose
        run: |
          if [ "${{ steps.check_changes_step.outputs.build_needed }}" == "true" ]; then
            echo "🚀 Major changes detected. Rebuilding..."
            docker compose up -d --build
          else
            echo "⚡ Minor changes detected. Smart updating..."
            # 서비스 이름을 특정하지 않아도 변경된 코드만 반영됩니다.
            docker compose up -d
          fi

      - name: Clean up unused images
        run: docker image prune -f